---
name: Call Trivy
on:
  push:
    branches: [main]
  pull_request:
jobs:
  scan:
    name: Scan
    uses: sloths-of-yore/actions/.github/workflows/reusable-trivy.yml@main
    with:
      image: 'ghcr.io/sloths-of-yore/wordsmith-web:latest'
